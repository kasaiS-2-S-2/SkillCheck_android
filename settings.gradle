dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 追記
        maven {
            url "https://jitpack.io"
        }
    }
}
rootProject.name = "SkillCheck"
include ':app'
